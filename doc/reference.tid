created: 20260317120000000
modified: 20260317120000000
rdt.hub-category: ui
rdt.hub-category-name: UI
rdt.hub-order: 1
rdt.order: 5
rdt.plugin-key: statewrap
rdt.plugin-name: statewrap
rdt.section: Reference
title: $:/plugins/rimir/statewrap/doc/reference
type: text/vnd.tiddlywiki

<$transclude $tiddler="$:/plugins/rimir/doc-template/templates/page">
<$fill $name="body">

!! Widget reference

!!! `<$statewrap>`

|!Attribute |!Required |!Description |
|`channels` |Yes |Space-separated channel names (`[a-z0-9-]`) |
|`default-<name>` |No |Default value for channel `<name>`. Omitted channels default to `""` |
|`instid` |No |Instance ID for state isolation. Defaults to TW `qualify` mechanism |

!!! `<$statewrap-rule>`

|!Attribute |!Required |!Description |
|`when` |Yes |Channel name that triggers this rule's actions |

Must be a descendant of `<$statewrap>` (any nesting depth). Contains action widgets that execute when the named channel changes.

!!! `<$action-statewrap-set>`

|!Attribute |!Required |!Description |
|`channel` |Yes |Channel name to write |
|`value` |No |Value to set (defaults to `""`) |
|`no-rules` |No |`"yes"` to skip reactive rules |

!! Filter operator reference

!!! `[statewrap-get[channel]]`

Returns the text value of the named channel's state tiddler. Requires a `<$statewrap>` ancestor (the operator walks up the widget tree to find the statewrap context). Returns empty if channel not found or no context.

!!! `[statewrap-ref[channel]]`

Returns the state tiddler title (path) for the named channel. Requires a `<$statewrap>` ancestor. Useful for widgets that need a tiddler reference (e.g., `$reveal stateTitle`).

!!! `[<instid>statewrap-lookup[channel]]`

Tree-independent read: takes the statewrap `instid` as filter input and the channel name as operand. Reads the state tiddler at `$:/state/rimir/statewrap/<instid>/<channel>` directly — no widget-tree walk, no parent `<$statewrap>` required.

Use this when `statewrap-get` can't work:

* Inside `\function` bodies (see the ''tw-function-fake-widget'' gotcha — `\function` runs on a fake widget without `parentWidget`)
* In the LLM chat's system-prompt template, which is wikified outside the app's statewrap scope
* In detached rendering (e.g., the `wikitext_render` llm-connect tool) — or use the `wikitext_render_in_app` tool variant, which wraps the render in a synthesized `<$statewrap>` so plain `statewrap-get` resolves
* Any filter expression that knows the instid explicitly and doesn't need the convenience of the tree walk

The lookup returns only the state tiddler's current text — it does not apply `default-<channel>` attributes (defaults are written to state tiddlers by `<$statewrap>` during its first render, so this matters only if the statewrap has never been rendered for that instid).

!! State tiddler path format

```
$:/state/rimir/statewrap/<instid>/<channel-name>
```

!! Edge cases

* ''Missing context'' — action widget and filter operators silently no-op if no `<$statewrap>` ancestor exists
* ''Unknown channel'' — logs a console warning, no-op
* ''No cascading'' — `_applyingRules` flag prevents rules from triggering further rules
* ''Shared instid'' — two `<$statewrap>` with the same `instid` share state tiddlers (intentional for cross-widget communication)
* ''Nested statewrap'' — child widgets find the nearest ancestor context

</$fill>
</$transclude>
